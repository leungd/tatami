{% extends 'base.twig' %}

{% block hero %}
  {% embed 'partials/hero.twig'
    with {
      title
    }
  %}
    {% block heroBody %}
      <h1>{{ title }}</h1>
    {% endblock %}
  {% endembed %}
{% endblock %}

{% block content %}
  <div class="fluid-grid">
    <div class="col-[content-start/content-end]">
      {% include 'partials/post-list.twig' with {
        empty_message: __('No results found for “%s”.', 'tatami')|format(
          search_query
        )
      } %}
    </div>
  </div>
{% endblock %}
