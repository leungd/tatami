{% extends 'base.twig' %}

{% block content %}
  <div class="fluid-grid">
    <div class="col-[content-start/content-end]">
      <div class="prose">
        {{ post.content|raw }}
      </div>
    </div>
  </div>
  {% include 'modules/faqs.twig' with {
    faqs: post.meta('faqs')
  } %}
{% endblock %}
